---
"@cuped-io/flame": patch
"@cuped-io/flame-react": patch
"@cuped-io/flame-edge": patch
---

Update README + JSDoc pointers from "Settings → Goals → Custom event" to "Project → Goals → Custom event" — goals are first-class project-scoped entities now, defined on a per-project Goals page rather than under account Settings. The runtime API is unchanged; this is a docs/JSDoc fix only.
