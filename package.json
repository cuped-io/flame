{
  "name": "@cuped-io/flame-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "cuped.io client-side SDKs (vanilla flame + framework bindings)",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter './packages/*' build",
    "test": "pnpm --filter './packages/*' test",
    "test:e2e": "pnpm build && pnpm --filter @cuped-io/e2e exec playwright test",
    "test:e2e:install": "pnpm --filter @cuped-io/e2e exec playwright install --with-deps chromium",
    "typecheck": "pnpm --filter './packages/*' typecheck",
    "lint": "eslint packages/*/src packages/*/scripts",
    "format": "prettier --write packages/*/src",
    "example:next": "pnpm --filter @cuped-io/example-next-app dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@eslint/js": "^9.39.2",
    "eslint": "^9.17.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.53.1"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx}": [
      "eslint --fix"
    ]
  }
}
